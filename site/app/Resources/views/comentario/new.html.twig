<form action="{{ path('comentario_new', {'id': comentario.Libro.id}) }}" method="POST">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Guardar" class="btn-primary" />
    {{ form_end(form) }}
    </form>
